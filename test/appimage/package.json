{
  "name": "rattin-appimage-validator",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright-over-CDP runtime check against the bundled Qt shell. Invoked by test/appimage/validator.sh from CI.",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
